<%= form_for(@roi) do |f| %>
  <% if @roi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roi.errors.count, "error") %> prohibited this roi from being saved:</h2>

      <ul>
      <% @roi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :toi_no %><br>
    <%= f.number_field :toi_no %>
  </div>
  <div class="field">
    <%= f.label :toi_name %><br>
    <%= f.text_field :toi_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
